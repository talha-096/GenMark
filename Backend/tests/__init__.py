# tests/__init__.py
# Test suite for the GenMark Backend.
# Run with: cd Backend && pytest
#
# Files:
#   conftest.py      →  Shared fixtures (test app, test client, mock DB)
#   test_health.py   →  Tests for the /api/health endpoint
